#include <stdio.h>
#include <stdin.h>
#include <string.h>
#include <sys/types.h>
#include <sys/shm.h>
#include <sys/sem.h>


int main(){
  int semkey = ftok("makefile" , 22);
  int shmkey = ftok("sem.c", 12);
  
  int semid = semget(semkey, 1, 0);
  
  semctl(semid, 0, -1,
}
